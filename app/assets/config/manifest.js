//= link_tree ../images
//= link ../builds/application.js
// Note: CSS files are built by PostCSS and linked directly via cssbundling-rails, not through the asset pipeline
// Source maps (.js.map) are automatically handled by Sprockets when linking the .js file
